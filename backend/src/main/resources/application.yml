server:
    port: 8086

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Wordle
    username: postgres
    password: pass1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true